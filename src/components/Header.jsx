

export function Header(){
    //siempre lleva un return porque lo que esta aca adentro
    //es lo que va a renderizar el react.
    //dentro de cada componente solo se puede utilizar una funcion.
    return (
        <div>

        </div>
    );
}